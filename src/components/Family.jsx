import React, { useContext } from "react";
import { DataContext } from "./Providers/AuthInfoProvider";

const Family = () => {
  //   const {  } = useContext(DataContext);

  return (
    <div>
      <h2>This is the family section </h2>
    </div>
  );
};

export default Family;
